enum CourseStatus {
  pending = 'pending',
  approved = 'approved',
  started = 'started',
  testing = 'testing',
  rejected = 'rejected',
  completed = 'completed',
}

export default CourseStatus;
