enum CourseStatus {
  pending = 'pending',
  approved = 'approved',
  started = 'started',
  testing = 'testing',
  rejected = 'rejected',
  successful = 'successful',
  completed = 'completed',
}

export default CourseStatus;
