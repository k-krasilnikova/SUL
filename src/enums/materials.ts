export enum ContentElementType {
  video = 'video',
  plain = 'plain',
  presentation = 'presentation',
}

export enum LessonType {
  video = 'video',
  text = 'text',
  presentation = 'presentation',
}
