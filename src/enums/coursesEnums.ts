enum CourseStatus {
  pending = 'pending',
  approved = 'approved',
  started = 'started',
  testing = 'testing',
  assessment = 'assessment',
  rejected = 'rejected',
  successful = 'successful',
  failed = 'failed',
  completed = 'completed',
}

export default CourseStatus;
