enum CourseStatus {
  pending = 'pending',
  approved = 'approved',
  started = 'started',
  testing = 'testing',
  assessment = 'assessment',
  rejected = 'rejected',
  completed = 'completed',
  failed = 'failed',
}

export default CourseStatus;
